# Completed Checkpoints

Use these folders when students fall behind:

- Lab2-Start: Lab 1 is already completed (TaskItem model, controller, CRUD views, navigation).
- Lab3-Start: Labs 1-2 are already completed (TaskItem feature + TaskItemService JSON persistence + DI wiring).
- Finished-Lab: reference final state for instructor fallback/demo.

Build and run commands (from each checkpoint folder):

- dotnet restore CopilotDemo.csproj
- dotnet build CopilotDemo.csproj
- dotnet run --project CopilotDemo.csproj
